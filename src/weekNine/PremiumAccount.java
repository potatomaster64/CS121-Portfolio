package weekNine;

public class PremiumAccount extends BankAccount{
    public PremiumAccount(String firstName, String lastName, String accountType, int age, double initialDeposit) {
        super(firstName, lastName, accountType, age, initialDeposit);
    }
}
